# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
frontend/dist/

# Runtime data (admin-uploaded robots/courses) — must survive `git pull`.
# Anchored to the repo root so it does NOT match the built-in platform
# catalogue under backend/app/data/.
/data/

# Logs — keep the directory, ignore the log files
logs/*
!logs/.gitkeep

# nginx basic-auth file (if ever used)
deploy/.htpasswd

# Editor / OS
.DS_Store
.vscode/
.idea/
